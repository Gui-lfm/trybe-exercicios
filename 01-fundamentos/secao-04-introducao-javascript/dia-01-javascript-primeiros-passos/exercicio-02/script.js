const base = 5;

const heigth = 8;

// resultado area retângulo:

const area = base * heigth;
console.log(`área do retângulo: ${area}`);

// perimetro do retângulo:

const perimetro = 2* (base + heigth)

console.log(`perimetro do retângulo: ${perimetro}`)