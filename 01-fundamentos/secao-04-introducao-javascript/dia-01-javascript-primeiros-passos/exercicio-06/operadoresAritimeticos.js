// Operações aritiméticas
const a = 8;
const b = 3;

    //soma
const soma = (a + b);

    //subtração
const subtracao = (a - b);

    //divisão
const divisao = (a / b);

    //multiplicação
const multiplicacao = (a * b);

    //resto divisão
const modulo = (a % b);

console.log(soma);
console.log(subtracao);
console.log(divisao);
console.log(multiplicacao);
console.log(modulo);
